/** Statistiques isolées d’un compte utilisateur. */
export interface UserAccountStats {
  documents: number;
  analyses: number;
  alerts: number;
  folders: number;
  tags: number;
  favorites: number;
}
