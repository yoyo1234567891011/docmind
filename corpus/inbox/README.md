# Inbox (PII — non versionné)

Déposez ici vos PDF / MD / TXT **originaux** puis :

```bash
npm run corpus:import -- --inbox --type Bail --subcategory bail
```

Les fichiers traités sont copiés dans `_processed/` (également ignoré par git).
